# 🚀 如何发布到 GitHub

## ⚠️ 重要提示

**在推送之前，需要先在 GitHub 上创建仓库！**

## 📋 发布步骤

### 步骤1: 在 GitHub 创建仓库

1. 访问 https://github.com/new
2. 填写信息：
   - **Repository name**: `github-autosync`
   - **Description**: `🔄 聊天式自动同步工具 - 一边聊天，一边自动同步到 GitHub（支持图形界面）`
   - 选择 **Public**
   - **不要**勾选 "Initialize this repository with a README"
3. 点击 **"Create repository"**

### 步骤2: 推送代码

创建仓库后，运行以下命令：

```powershell
cd C:\Users\10250\OneDrive\Desktop\GitHub-AutoSync

# 设置远程仓库（使用你的 Token）
git remote set-url origin https://YOUR_TOKEN_HERE@github.com/mm1025048717-hash/github-autosync.git

# 推送到 GitHub
git push -u origin main
```

### 步骤3: 或者使用部署脚本

```powershell
.\deploy-with-token.ps1 -RepoUrl "https://github.com/mm1025048717-hash/github-autosync.git" -Token "YOUR_TOKEN_HERE"
```

## ✅ 发布后操作

1. **添加 Topics（标签）**：
   - `github`
   - `autosync`
   - `powershell`
   - `electron`
   - `git`
   - `automation`
   - `developer-tools`
   - `cursor`

2. **创建 Release v1.0.0**

3. **分享给其他人使用**

---

**现在代码已经准备好了，创建仓库后即可发布！** 🎉
